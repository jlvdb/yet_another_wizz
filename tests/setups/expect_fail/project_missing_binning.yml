# yaw_cli v3.0.3 configuration

correlation:
    scales:
        rmin: 100
        rmax: 1000
        unit: kpc
    max_workers: 11

inputs:
    reference:
        path_data: $DATA$
        path_rand: $RAND$
        ra: ra
        dec: dec
        redshift: z
    unknown:
        path_data:
            1: $DATA$
        path_rand:
            1: $RAND$
        ra: ra
        dec: dec
        weight: weight
        redshift: z
    num_patches: 11

tasks:
  - auto_ref
  - auto_unk
  - cross_corr
  - estimate
  - hist
  - plot
