#!/usr/bin/env python3
from yaw.pipe.parser import parser
from yaw.pipe import tasks


def main():
    args = parser.parse_args()
    if args.task is None:
        parser.print_usage()
    else:  # run
        func = getattr(tasks, args.task)
        func(args)


if __name__ == "__main__":
    main()
